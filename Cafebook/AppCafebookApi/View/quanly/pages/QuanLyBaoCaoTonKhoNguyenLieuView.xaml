<Page x:Class="AppCafebookApi.View.quanly.pages.QuanLyBaoCaoTonKhoNguyenLieuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản Lý Báo Cáo Tồn Kho Nguyên Liệu"
      Background="#FFF8E1" FontFamily="Segoe UI"
      Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>

        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="KpiCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Color="#000" Opacity="0.08" BlurRadius="15"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Báo Cáo Tồn Kho Nguyên Liệu" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}"/>
            <Button Content="🡄 Quay lại" HorizontalAlignment="Right" Style="{StaticResource ActionBtn}" Background="Gray" Click="BtnQuayLai_Click"/>
        </Grid>

        <Border Grid.Row="1" Background="White" Padding="15" Margin="0,0,0,10" CornerRadius="8">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Tên Nguyên liệu:" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="txtSearch" Width="150" VerticalAlignment="Center" Padding="5" Margin="0,0,15,0"/>

                <TextBlock Text="Nhà cung cấp:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox x:Name="cmbNhaCungCap" Width="150" DisplayMemberPath="Ten" SelectedValuePath="Id" VerticalAlignment="Center" Margin="0,0,15,0"/>

                <CheckBox x:Name="chkLowStock" Content="Chỉ hiện sắp hết" VerticalAlignment="Center" Margin="0,0,20,0" Foreground="{StaticResource DarkBrownBrush}"/>

                <Button x:Name="btnGenerate" Content="⟳ Tạo Báo Cáo" Style="{StaticResource ActionBtn}" Margin="0,0,10,0" Click="BtnGenerate_Click"/>
                <Button x:Name="btnExportToExcel" Content="⬇ Xuất Excel" Style="{StaticResource ActionBtn}" Background="#2E7D32" Click="BtnExportToExcel_Click"/>
            </WrapPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource KpiCard}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock x:Name="lblTongGiaTriTonKho" Text="0" FontSize="24" FontWeight="Bold" Foreground="#42A5F5"/>
                    <TextBlock Text="Tổng Giá Trị Tồn Kho Ước Tính" Foreground="{StaticResource TextGrayBrush}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource KpiCard}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock x:Name="lblSoLuongSPSapHet" Text="0" FontSize="24" FontWeight="Bold" Foreground="#FFA726"/>
                    <TextBlock Text="Số Lượng NL Sắp Hết" Foreground="{StaticResource TextGrayBrush}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource KpiCard}" Margin="0">
                <StackPanel>
                    <TextBlock x:Name="lblTongGiaTriDaHuy" Text="0" FontSize="24" FontWeight="Bold" Foreground="#EF5350"/>
                    <TextBlock Text="Tổng Giá Trị Đã Hủy" Foreground="{StaticResource TextGrayBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <TabControl Grid.Row="3" Margin="0,10,0,0" BorderThickness="0" Background="Transparent">
            <TabItem Header="Chi Tiết Tồn Kho">
                <DataGrid x:Name="dgChiTietTonKho" Background="White" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nguyên Liệu" Binding="{Binding TenNguyenLieu}" Width="*"/>
                        <DataGridTextColumn Header="Đơn Vị" Binding="{Binding DonViTinh}" Width="100"/>
                        <DataGridTextColumn Header="Tồn Hiện Tại" Binding="{Binding TonKho, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Tồn Tối Thiểu" Binding="{Binding TonKhoToiThieu, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Tình Trạng" Binding="{Binding TinhTrang}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TinhTrang}" Value="Sắp hết">
                                            <Setter Property="Foreground" Value="Red"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Lịch Sử Kiểm Kê">
                <DataGrid x:Name="dgLichSuKiemKe" Background="White" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ngày Kiểm" Binding="{Binding NgayKiem, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                        <DataGridTextColumn Header="Nguyên Liệu" Binding="{Binding TenNguyenLieu}" Width="*"/>
                        <DataGridTextColumn Header="Tồn Hệ Thống" Binding="{Binding TonKhoHeThong, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Tồn Thực Tế" Binding="{Binding TonKhoThucTe, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Chênh Lệch" Binding="{Binding ChenhLech, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Lý do" Binding="{Binding LyDoChenhLech}" Width="250"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Lịch Sử Hủy Hàng">
                <DataGrid x:Name="dgLichSuHuyHang" Background="White" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ngày Hủy" Binding="{Binding NgayHuy, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                        <DataGridTextColumn Header="Nguyên Liệu" Binding="{Binding TenNguyenLieu}" Width="*"/>
                        <DataGridTextColumn Header="Số Lượng Hủy" Binding="{Binding SoLuongHuy, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Giá trị Hủy" Binding="{Binding GiaTriHuy, StringFormat=N0}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="Red"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Lý do Hủy" Binding="{Binding LyDoHuy}" Width="250"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#80FFFFFF" Visibility="Collapsed" CornerRadius="8">
            <TextBlock Text="Đang xử lý..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}"/>
        </Border>
    </Grid>
</Page>