<Page x:Class="AppCafebookApi.View.quanly.pages.QuanLyBanView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Bàn" FontFamily="Segoe UI" Background="#FFF8E1" Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#EF5350"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#BDBDBD"/>
        <SolidColorBrush x:Key="BackgroundHoverBrush" Color="#F5F5F5"/>

        <Geometry x:Key="IconAddTable">M4,6H20V8H4V6M4,10H20V12H4V10M4,14H11V16H4V14M19,16V19H16V21H14V19H11V16H14V14H16V16H19Z</Geometry>
        <Geometry x:Key="IconSave">M17,3H5C3.89,3 3,3.9 3,5V19C3,20.1 3.9,21 5,21H19C20.1,21 21,20.1 21,19V7L17,3M15,9H5V5H15V9M12,19C10.34,19 9,17.66 9,16C9,14.34 10.34,13 12,13C13.66,13 15,14.34 15,16C15,17.66 13.66,19 12,19Z</Geometry>
        <Geometry x:Key="IconDelete">M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19V4M6,19C6,20.1 6.9,21 8,21H16C17.1,21 18,20.1 18,19V7H6V19Z</Geometry>
        <Geometry x:Key="IconHistory">M13.5,8H12V13L16.28,15.54L17,14.33L13.5,12.25V8M13,3C8.03,3 4,7.03 4,12C4,16.97 8.03,21 13,21C17.97,21 22,16.97 22,12C22,7.03 17.97,3 13,3M13,19C9.13,19 6,15.87 6,12C6,8.13 9.13,5 13,5C16.87,5 20,8.13 20,12C20,15.87 16.87,19 13,19Z</Geometry>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
        </Style>
        <Style x:Key="MaterialTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGrayBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style x:Key="MaterialComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGrayBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="QUẢN LÝ SƠ ĐỒ BÀN" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnNavKhuVuc" Style="{StaticResource ActionButton}" Background="#1976D2" Margin="0,0,10,0" Click="BtnNavKhuVuc_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Quản lý Khu vực"/>
                    </StackPanel>
                </Button>
                <Button x:Name="btnNavSuCo" Style="{StaticResource ActionButton}" Background="#FF9800" Click="BtnNavSuCo_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Xem Sự cố Bàn"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <Border Background="White" CornerRadius="8" Margin="0,0,10,0" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Khu vực:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox x:Name="cmbFilterKV" Width="150" DisplayMemberPath="TenKhuVuc" SelectedValuePath="IdKhuVuc" Style="{StaticResource MaterialComboBox}" SelectionChanged="Filter_Changed"/>
                        <TextBlock Text="Tìm bàn:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                        <TextBox x:Name="txtSearch" Width="150" Style="{StaticResource MaterialTextBox}" TextChanged="Filter_Changed"/>
                        <Button x:Name="btnThemMoi" Margin="15,0,0,0" Style="{StaticResource ActionButton}" Background="#4CAF50" Click="BtnThemMoi_Click">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconAddTable}" Fill="White" Width="16" Height="16" Margin="0,0,5,0" Stretch="Uniform"/>
                                <TextBlock Text="Thêm mới"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <DataGrid x:Name="dgBan" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DgBan_SelectionChanged" BorderThickness="0" Background="Transparent">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Số Bàn" Binding="{Binding SoBan}" Width="100"/>
                            <DataGridTextColumn Header="Khu Vực" Binding="{Binding TenKhuVuc}" Width="150"/>
                            <DataGridTextColumn Header="Số Ghế" Binding="{Binding SoGhe}" Width="80"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Background="White" CornerRadius="8" Grid.Column="1" Padding="20">
                <StackPanel x:Name="formChiTiet" IsEnabled="False">
                    <TextBlock x:Name="lblTitle" Text="Chi tiết Bàn" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,20"/>

                    <TextBlock Text="Thuộc Khu Vực" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,5"/>
                    <ComboBox x:Name="cmbKhuVuc" DisplayMemberPath="TenKhuVuc" SelectedValuePath="IdKhuVuc" Style="{StaticResource MaterialComboBox}"/>

                    <TextBlock Text="Số Bàn (Tên bàn)" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,15,0,5"/>
                    <TextBox x:Name="txtSoBan" Style="{StaticResource MaterialTextBox}"/>

                    <TextBlock Text="Số Ghế" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,15,0,5"/>
                    <TextBox x:Name="txtSoGhe" Style="{StaticResource MaterialTextBox}"/>

                    <TextBlock Text="Trạng thái (Khóa Bàn)" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,15,0,5"/>
                    <ComboBox x:Name="cmbTrangThai" Style="{StaticResource MaterialComboBox}">
                        <ComboBoxItem Content="Trống"/>
                        <ComboBoxItem Content="Đang phục vụ"/>
                        <ComboBoxItem Content="Bảo trì"/>
                        <ComboBoxItem Content="Tạm ngưng"/>
                    </ComboBox>

                    <TextBlock Text="Ghi chú" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,15,0,5"/>
                    <TextBox x:Name="txtGhiChu" Style="{StaticResource MaterialTextBox}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                        <Button x:Name="btnLichSu" Click="BtnLichSu_Click" Margin="0,0,10,0" Style="{StaticResource ActionButton}" Background="#42A5F5" Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconHistory}" Fill="White" Width="18" Height="18" Margin="0,0,5,0" Stretch="Uniform"/>
                                <TextBlock Text="Lịch sử"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="btnLuu" Click="BtnLuu_Click" Margin="0,0,10,0" Style="{StaticResource ActionButton}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconSave}" Fill="White" Width="18" Height="18" Margin="0,0,5,0" Stretch="Uniform"/>
                                <TextBlock Text="Lưu"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="btnXoa" Click="BtnXoa_Click" Style="{StaticResource DeleteButton}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconDelete}" Fill="White" Width="18" Height="18" Margin="0,0,5,0" Stretch="Uniform"/>
                                <TextBlock Text="Xóa"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border x:Name="LoadingOverlay" Grid.ColumnSpan="2" Background="#80FFFFFF" Visibility="Collapsed">
                <TextBlock Text="Đang xử lý..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
            </Border>
        </Grid>
    </Grid>
</Page>