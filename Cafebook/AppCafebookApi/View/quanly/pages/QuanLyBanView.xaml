<Page x:Class="AppCafebookApi.View.quanly.pages.QuanLyBanView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Bàn" FontFamily="Segoe UI" Background="#FFF8E1" Loaded="Page_Loaded">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0" Duration="0:0:0.5">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>
    
    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#EF5350"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#BDBDBD"/>
        <SolidColorBrush x:Key="BackgroundHoverBrush" Color="#F5F5F5"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="QUẢN LÝ SƠ ĐỒ BÀN" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="btnNavKhuVuc" Content="🗺 Quản lý Khu vực" Style="{StaticResource ActionButton}" Background="#1976D2" Margin="0,0,5,0" Click="BtnNavKhuVuc_Click"/>
                <Button x:Name="btnNavSuCo" Content="🛠 Quản lý Sự cố" Style="{StaticResource ActionButton}" Background="#FF9800" Margin="0,0,15,0" Click="BtnNavSuCo_Click"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="GridDuLieuBan" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Khu vực:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox x:Name="cmbFilterKhuVuc" Width="150" DisplayMemberPath="TenKhuVuc" SelectedValuePath="IdKhuVuc" SelectionChanged="Filter_Changed" Padding="5"/>
                        <Button x:Name="btnThem" Content="➕ Thêm mới" Style="{StaticResource ActionButton}" Background="#4CAF50" Margin="15,0,0,0" Click="BtnLamMoi_Click"/>
                    </StackPanel>

                    <DataGrid x:Name="dgBan" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                              SelectionMode="Single" SelectionChanged="DgBan_SelectionChanged"
                              BorderThickness="0" Background="Transparent" AlternatingRowBackground="#FAFAFA">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Số Bàn" Binding="{Binding SoBan}" Width="80"/>
                            <DataGridTextColumn Header="Khu vực" Binding="{Binding TenKhuVuc}" Width="120"/>
                            <DataGridTextColumn Header="Số Ghế" Binding="{Binding SoGhe}" Width="70"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20">
                <StackPanel x:Name="formChiTiet" IsEnabled="False">
                    <TextBlock x:Name="lblTitle" Text="Thông tin Bàn" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,20"/>

                    <TextBlock Text="Số Bàn (*)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtSoBan" Padding="8" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,15"/>

                    <TextBlock Text="Khu vực (*)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbKhuVuc" DisplayMemberPath="TenKhuVuc" SelectedValuePath="IdKhuVuc" Padding="8" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,15"/>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,5,0">
                            <TextBlock Text="Số Ghế" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtSoGhe" Padding="8" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="0,0,0,1"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Text="Trạng thái" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cmbTrangThai" Padding="8" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="0,0,0,1">
                                <ComboBoxItem Content="Trống"/>
                                <ComboBoxItem Content="Đang phục vụ"/>
                                <ComboBoxItem Content="Đã đặt trước"/>
                                <ComboBoxItem Content="Bảo trì"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Ghi chú" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtGhiChu" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="8" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="1" Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnLichSu" Content="📜 Lịch sử" Style="{StaticResource ActionButton}" Background="#9E9E9E" Margin="0,0,10,0" Click="BtnLichSu_Click"/>
                        <Button x:Name="btnLuu" Content="💾 Lưu" Style="{StaticResource ActionButton}" Margin="0,0,10,0" Click="BtnLuu_Click"/>
                        <Button x:Name="btnXoa" Content="🗑 Xóa" Style="{StaticResource DeleteButton}" Click="BtnXoa_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Border x:Name="txtThongBaoKhongCoQuyen" 
                Grid.Row="1" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Background="White" 
                CornerRadius="15" 
                Padding="60,40" 
                Visibility="Collapsed">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="4" BlurRadius="25" Opacity="0.1" Color="Black"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="🛡️" FontSize="75" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBlock Text="TRUY CẬP BỊ HẠN CHẾ" FontSize="22" FontWeight="Bold" Foreground="#D32F2F" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Bạn không có quyền quản lý sơ đồ bàn." FontSize="16" Foreground="#424242" HorizontalAlignment="Center"/>
                <TextBlock Text="Vui lòng sử dụng các nút chức năng phía trên thanh công cụ." FontSize="15" Foreground="Gray" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="2" Background="#80FFFFFF" Visibility="Collapsed">
            <TextBlock Text="Đang xử lý..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
        </Border>
    </Grid>
</Page>