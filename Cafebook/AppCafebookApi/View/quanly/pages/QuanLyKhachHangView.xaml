<Page x:Class="AppCafebookApi.View.quanly.pages.QuanLyKhachHangView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Khách hàng" FontFamily="Segoe UI" Background="#FFF8E1" Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#BDBDBD"/>

        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#6F4E37"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteBtn" TargetType="Button" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#F44336"/>
        </Style>

        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#42A5F5"/>
            <Setter Property="Padding" Value="5,2,5,4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#1E88E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="QUẢN LÝ KHÁCH HÀNG" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnNavKhuyenMai" Content="🎁 Khuyến Mãi" Style="{StaticResource ActionBtn}" Background="#2196F3" Margin="0,0,10,0" Click="BtnNavKhuyenMai_Click"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="GridDuLieu" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="450"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="14"/>
                        <TextBox x:Name="txtSearch" Width="150" Style="{StaticResource SearchTextBoxStyle}" TextChanged="Filters_Changed"/>

                        <TextBlock Text="Loại KH:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                        <ComboBox x:Name="cmbLoaiTK" Width="100" SelectionChanged="Filters_Changed" SelectedIndex="0" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderGrayBrush}">
                            <ComboBoxItem Content="Tất cả"/>
                            <ComboBoxItem Content="Khách vãng lai"/>
                            <ComboBoxItem Content="Thành viên"/>
                        </ComboBox>

                        <CheckBox x:Name="chkHideLocked" Content="Ẩn khóa" VerticalAlignment="Center" Margin="15,0,0,0" Checked="Filters_Changed" Unchecked="Filters_Changed"/>

                        <Button x:Name="btnExportExcel" Content="📊 Xuất Excel" Style="{StaticResource ActionBtn}" Background="#00724C" Margin="15,0,0,0" Padding="10,5" Click="BtnExportExcel_Click"/>
                    </StackPanel>

                    <DataGrid x:Name="dgKhachHang" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0" Background="Transparent" SelectionChanged="DgKhachHang_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã" Binding="{Binding IdKhachHang}" Width="40"/>
                            <DataGridTextColumn Header="Khách hàng" Binding="{Binding HoTen}" Width="*"/>
                            <DataGridTextColumn Header="SĐT" Binding="{Binding SoDienThoai}" Width="90"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="140"/>
                            <DataGridTextColumn Header="Loại TK" Binding="{Binding LoaiTaiKhoan}" Width="90"/>
                            <DataGridTextColumn Header="Điểm" Binding="{Binding DiemTichLuy}" Width="50"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="formChiTiet" IsEnabled="False">
                        <TextBlock x:Name="lblFormTitle" Text="Thông tin Khách Hàng" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="1" Width="80" Height="80" Background="#F5F5F5" CornerRadius="40" Cursor="Hand" MouseLeftButtonDown="Avatar_Click" ToolTip="Nhấn xem ảnh phóng to">
                                <Grid>
                                    <Image x:Name="AvatarPreview" Stretch="UniformToFill">
                                        <Image.Clip>
                                            <EllipseGeometry RadiusX="40" RadiusY="40" Center="40,40"/>
                                        </Image.Clip>
                                    </Image>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Họ Tên" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtHoTen" IsReadOnly="True" Background="#F5F5F5" BorderThickness="0" Padding="5"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,5,0">
                                <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtSdt" IsReadOnly="True" Background="#F5F5F5" BorderThickness="0" Padding="5"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="Điểm Tích Lũy" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox x:Name="txtDiem" Width="60" IsReadOnly="True" Background="#E3F2FD" BorderThickness="0" Padding="5" Foreground="#2196F3" FontWeight="Bold"/>
                                    <Button x:Name="btnSuaDiem" Content="Cập nhật" Style="{StaticResource ActionBtn}" Background="#2196F3" Padding="8,4" Margin="5,0,0,0" Click="BtnSuaDiem_Click" Visibility="Collapsed"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,5,0,0"/>
                        <TextBox x:Name="txtEmail" IsReadOnly="True" Background="#F5F5F5" BorderThickness="0" Margin="0,0,0,10" Padding="5"/>

                        <TextBlock Text="Địa chỉ" FontWeight="SemiBold" Margin="0,5,0,0"/>
                        <TextBox x:Name="txtDiaChi" Height="50" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True" Background="#F5F5F5" BorderThickness="0" Margin="0,0,0,10" Padding="5"/>

                        <TextBlock x:Name="txtTrangThai" Text="Trạng thái: Hoạt động" FontWeight="Bold" Foreground="#4CAF50" Margin="0,5,0,20"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
                            <Button x:Name="btnMoKhoa" Content="🔓 Mở Khóa" Style="{StaticResource ActionBtn}" Background="#4CAF50" Margin="0,0,10,0" Click="BtnMoKhoa_Click" Visibility="Collapsed"/>
                            <Button x:Name="btnKhoa" Content="🔒 Khóa Tài Khoản" Style="{StaticResource ActionBtn}" Background="#FF9800" Margin="0,0,10,0" Click="BtnKhoa_Click" Visibility="Collapsed"/>
                            <Button x:Name="btnXoa" Content="🗑 Xóa Mềm" Style="{StaticResource DeleteBtn}" Click="BtnXoa_Click" Visibility="Collapsed"/>
                        </StackPanel>

                        <StackPanel x:Name="panelLichSu" Visibility="Collapsed">
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border x:Name="PopupKhoa" Grid.RowSpan="2" Background="#80000000" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Background="White" Width="380" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="25" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="🔒 KHÓA TÀI KHOẢN" FontSize="20" FontWeight="Bold" Foreground="#F44336" Margin="0,0,0,15"/>
                    <TextBlock Text="Thời gian khóa:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbThoiGianKhoa" SelectedIndex="0" Margin="0,0,0,15">
                        <ComboBoxItem Content="1 Ngày" Tag="1"/>
                        <ComboBoxItem Content="3 Ngày" Tag="3"/>
                        <ComboBoxItem Content="7 Ngày" Tag="7"/>
                        <ComboBoxItem Content="Vĩnh viễn" Tag="0"/>
                    </ComboBox>
                    <TextBlock Text="Lý do khóa (Gửi email cho khách):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtLyDoKhoa" Height="70" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Hủy" Style="{StaticResource ActionBtn}" Background="Gray" Margin="0,0,10,0" Click="BtnHuyPopup_Click"/>
                        <Button Content="Xác nhận" Style="{StaticResource DeleteBtn}" Click="BtnXacNhanKhoa_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <Border x:Name="PopupDiem" Grid.RowSpan="2" Background="#80000000" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Background="White" Width="300" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="25" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="CẬP NHẬT ĐIỂM" FontSize="18" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,15"/>
                    <TextBlock Text="Nhập số điểm (+/-):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtDiemThayDoi" Margin="0,0,0,15" Padding="5"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Hủy" Style="{StaticResource ActionBtn}" Background="Gray" Margin="0,0,10,0" Click="BtnHuyPopup_Click"/>
                        <Button Content="Lưu" Style="{StaticResource ActionBtn}" Background="#2196F3" Click="BtnXacNhanDiem_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <Border x:Name="PopupXemAnh" Grid.RowSpan="2" Background="#E6000000" Visibility="Collapsed" Panel.ZIndex="200" MouseLeftButtonDown="BtnHuyPopup_Click">
            <Grid>
                <Image x:Name="ImgFullAvatar" Stretch="Uniform" Margin="40"/>
                <Button Content="✕ Đóng" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20" Background="Transparent" Foreground="White" FontSize="18" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnHuyPopup_Click"/>
            </Grid>
        </Border>

        <Border x:Name="txtThongBaoKhongCoQuyen" 
                Grid.Row="1" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Background="White" 
                CornerRadius="15" 
                Padding="60,40" 
                Visibility="Collapsed" Panel.ZIndex="300">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="4" BlurRadius="25" Opacity="0.1" Color="Black"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="🛡️" FontSize="75" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBlock Text="TRUY CẬP BỊ HẠN CHẾ" FontSize="22" FontWeight="Bold" Foreground="#D32F2F" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Bạn không có quyền xem hoặc chỉnh sửa hồ sơ khách hàng." FontSize="16" Foreground="#424242" HorizontalAlignment="Center"/>
                <TextBlock Text="Vui lòng sử dụng các nút chức năng phía trên thanh công cụ." FontSize="15" Foreground="Gray" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="2" Background="#80FFFFFF" Visibility="Collapsed" Panel.ZIndex="400">
            <TextBlock Text="Đang xử lý..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
        </Border>
    </Grid>
</Page>