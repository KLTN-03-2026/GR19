<Page x:Class="AppCafebookApi.View.quanly.pages.QuanLyNhanVienView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Nhân viên" FontFamily="Segoe UI" Background="#FFF8E1" Loaded="Page_Loaded">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0" Duration="0:0:0.5">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#BDBDBD"/>
        <SolidColorBrush x:Key="StatusRedBrush" Color="#FFEBEE"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#EF5350"/>

        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#6F4E37"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteBtn" TargetType="Button" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#EF5350"/>
        </Style>

        <Style x:Key="MaterialInput" TargetType="Control">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGrayBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialInput}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialInput}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialInput}"/>
        <Style TargetType="DatePicker" BasedOn="{StaticResource MaterialInput}"/>

        <Style x:Key="EmployeeDataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThaiLamViec}" Value="Nghỉ việc">
                    <Setter Property="Background" Value="{StaticResource StatusRedBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                    <Setter Property="FontStyle" Value="Italic"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="QUẢN LÝ NHÂN VIÊN" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}" VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="btnPhanQuyen" Content="🔑 Phân Quyền" Style="{StaticResource ActionBtn}" Background="#1976D2" Margin="0,0,5,0" Click="BtnPhanQuyen_Click"/>
                <Button x:Name="btnGoToLichLamViec" Content="🗓 Lịch Làm Việc" Style="{StaticResource ActionBtn}" Background="#4CAF50" Margin="0,0,5,0" Click="BtnGoToLichLamViec_Click"/>
                <Button x:Name="btnGoToDonXinNghi" Content="⛱ Xin Nghỉ" Style="{StaticResource ActionBtn}" Background="#9C27B0" Margin="0,0,5,0" Click="BtnGoToDonXinNghi_Click"/>
                <Button x:Name="btnGoToBaoCao" Content="📊 Báo Cáo" Style="{StaticResource ActionBtn}" Background="#FF9800" Margin="0,0,5,0" Click="BtnGoToBaoCao_Click"/>
                <Button x:Name="btnGoToHieuSuat" Content="📈 Hiệu Suất" Style="{StaticResource ActionBtn}" Background="#00BCD4" Margin="0,0,5,0" Click="BtnGoToHieuSuat_Click"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="GridDuLieuNhanVien" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="420"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="txtSearchNhanVien" Width="150" TextChanged="Filter_Changed"/>

                        <TextBlock Text="Vai trò:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                        <ComboBox x:Name="cmbFilterVaiTro" Width="150" SelectionChanged="Filter_Changed"/>

                        <Button x:Name="btnLamMoiForm" Content="➕ Thêm mới" Style="{StaticResource ActionBtn}" Background="#4CAF50" Margin="15,0,0,0" Click="BtnLamMoiForm_Click"/>
                    </StackPanel>

                    <DataGrid x:Name="dgNhanVien" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                              SelectionMode="Single" SelectionChanged="DgNhanVien_SelectionChanged"
                              BorderThickness="0" Background="Transparent"
                              RowStyle="{StaticResource EmployeeDataGridRowStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding IdNhanVien}" Width="50"/>
                            <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="*"/>
                            <DataGridTextColumn Header="Vai Trò" Binding="{Binding TenVaiTro}" Width="100"/>
                            <DataGridTextColumn Header="Lương/Giờ" Binding="{Binding LuongCoBan, StringFormat=N0}" Width="90"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThaiLamViec}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock x:Name="lblFormTitle" Text="Thông tin Nhân viên" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>

                        <Grid x:Name="formChiTiet" IsEnabled="False">
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="1" Width="100" Height="100" Background="#F5F5F5" CornerRadius="50">
                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <ImageBrush x:Name="AvatarPreview" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                        </Ellipse>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Orientation="Horizontal" Margin="15,0,0,0">
                                        <Button x:Name="btnChonAnh" Content="📷 Chọn ảnh" Style="{StaticResource ActionBtn}" Background="#E0E0E0" Foreground="Black" Click="BtnChonAnh_Click" Margin="0,0,10,0"/>
                                        <Button x:Name="btnXoaAnh" Content="🗑 Xóa ảnh" Style="{StaticResource DeleteBtn}" Click="BtnXoaAnh_Click"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Họ Tên (*)" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtHoTen" Margin="0,0,0,10"/>

                                <TextBlock Text="Tên Đăng Nhập (*)" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtTenDangNhap" Margin="0,0,0,10"/>

                                <TextBlock x:Name="lblMatKhau" Text="Mật Khẩu (*)" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <PasswordBox x:Name="txtMatKhau"/>
                                <TextBlock x:Name="lblMatKhauInfo" Text="Bỏ trống nếu không muốn đổi mật khẩu" FontSize="11" FontStyle="Italic" Foreground="Gray" Margin="0,2,0,10" Visibility="Collapsed"/>

                                <TextBlock Text="Vai Trò (*)" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <ComboBox x:Name="cmbVaiTro" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>

                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="Lương/Giờ (*)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                        <TextBox x:Name="txtLuongCoBan"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Trạng thái (*)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="cmbTrangThai">
                                            <ComboBoxItem Content="Đang làm việc"/>
                                            <ComboBoxItem Content="Nghỉ việc"/>
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Ngày Vào Làm" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <DatePicker x:Name="dpNgayVaoLam" Margin="0,0,0,10"/>

                                <TextBlock Text="Số Điện Thoại" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtSoDienThoai" Margin="0,0,0,10"/>

                                <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtEmail" Margin="0,0,0,10"/>

                                <TextBlock Text="Địa Chỉ" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtDiaChi" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel x:Name="panelActions" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="btnThem" Content="➕ Thêm Nhân Viên" Click="BtnThem_Click" Style="{StaticResource ActionBtn}" Background="#4CAF50" Padding="20,10" Margin="0,0,10,0"/>
                            <Button x:Name="btnLuu" Content="💾 Lưu Thay Đổi" Click="BtnLuu_Click" Style="{StaticResource ActionBtn}" Background="#1976D2" Padding="20,10" Margin="0,0,10,0"/>
                            <Button x:Name="btnCaiDatTrangThai" Content="⚙ Đổi TT" Visibility="Collapsed" Style="{StaticResource ActionBtn}" Background="Gray" Margin="0,0,10,0"/>
                            <Button x:Name="btnXoa" Content="🗑 Xóa" Click="BtnXoa_Click" Style="{StaticResource DeleteBtn}" Padding="20,10"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border x:Name="txtThongBaoKhongCoQuyen" 
                Grid.Row="1" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Background="White" 
                CornerRadius="15" 
                Padding="60,40" 
                Visibility="Collapsed">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="4" BlurRadius="25" Opacity="0.1" Color="Black"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="🛡️" FontSize="75" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBlock Text="TRUY CẬP BỊ HẠN CHẾ" FontSize="22" FontWeight="Bold" Foreground="#D32F2F" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Bạn không có quyền xem hoặc chỉnh sửa hồ sơ nhân viên." FontSize="16" Foreground="#424242" HorizontalAlignment="Center"/>
                <TextBlock Text="Vui lòng sử dụng các nút chức năng phía trên thanh công cụ." FontSize="15" Foreground="Gray" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="2" Background="#80FFFFFF" Visibility="Collapsed">
            <TextBlock Text="Đang xử lý..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
        </Border>
    </Grid>
</Page>