<Page x:Class="AppCafebookApi.View.quanly.pages.QuanLyBaoCaoDoanhThuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản Lý Báo Cáo Doanh Thu" 
      Background="#FFF8E1"
      FontFamily="Segoe UI"
      Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#EF5350"/>

        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="KpiCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Color="#000" Opacity="0.08" BlurRadius="15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="KpiValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="KpiTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextGrayBrush}"/>
        </Style>
    </Page.Resources>

    <Grid Opacity="1" x:Name="MainGrid" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Quản Lý Báo Cáo Doanh Thu" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}"/>
            <Button Content="🡄 Quay lại" HorizontalAlignment="Right" Style="{StaticResource ActionBtn}" Background="Gray" Click="BtnQuayLai_Click"/>
        </Grid>

        <Border Grid.Row="1" Background="White" Padding="15" Margin="0,0,0,10" CornerRadius="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Từ ngày:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker x:Name="dpStartDate" Width="150" VerticalAlignment="Center"/>
                <TextBlock Text="Đến ngày:" VerticalAlignment="Center" Margin="20,5,5,5"/>
                <DatePicker x:Name="dpEndDate" Width="150" VerticalAlignment="Center"/>

                <Button x:Name="btnGenerate" Content="⟳ Tạo Báo Cáo" Margin="20,0,0,0" Style="{StaticResource ActionBtn}" Click="BtnGenerate_Click"/>
                <Button x:Name="btnExportToExcel" Content="⬇ Xuất Excel" Margin="10,0,0,0" Style="{StaticResource ActionBtn}" Background="#2E7D32" Click="BtnExportToExcel_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource KpiCard}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock x:Name="lblDoanhThuRong" Text="0" Style="{StaticResource KpiValue}" Foreground="#42A5F5"/>
                    <TextBlock Text="Doanh Thu Ròng" Style="{StaticResource KpiTitle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource KpiCard}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock x:Name="lblTongGiaVon" Text="0" Style="{StaticResource KpiValue}" Foreground="#EF5350"/>
                    <TextBlock Text="Tổng Giá Vốn (COGS)" Style="{StaticResource KpiTitle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource KpiCard}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock x:Name="lblLoiNhuanGop" Text="0" Style="{StaticResource KpiValue}" Foreground="#FFA726"/>
                    <TextBlock Text="Lợi Nhuận Gộp" Style="{StaticResource KpiTitle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Style="{StaticResource KpiCard}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock x:Name="lblChiPhiOpex" Text="0" Style="{StaticResource KpiValue}" Foreground="#EF5350"/>
                    <TextBlock Text="Chi Phí Vận Hành" Style="{StaticResource KpiTitle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="4" Style="{StaticResource KpiCard}" Margin="0">
                <StackPanel>
                    <TextBlock x:Name="lblLoiNhuanRong" Text="0" Style="{StaticResource KpiValue}" Foreground="#66BB6A"/>
                    <TextBlock Text="Lợi Nhuận Ròng (Thực tế)" Style="{StaticResource KpiTitle}"/>
                </StackPanel>
            </Border>
        </Grid>

        <TabControl Grid.Row="3" Margin="0,10,0,0" BorderThickness="0" Background="Transparent">

            <TabItem Header="Top Sản phẩm">
                <DataGrid x:Name="dgTopSanPham" Background="White" BorderThickness="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên Sản Phẩm" Binding="{Binding TenSanPham}" Width="*"/>
                        <DataGridTextColumn Header="Đã Bán" Binding="{Binding TongSoLuongBan}" Width="150"/>
                        <DataGridTextColumn Header="Doanh Thu" Binding="{Binding TongDoanhThu, StringFormat=N0}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Chi tiết Doanh thu">
                <DataGrid x:Name="dgChiTietDoanhThu" Background="White" BorderThickness="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Chỉ số" Binding="{Binding Key}" Width="*" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Giá trị" Binding="{Binding Value, StringFormat=N0}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Gợi ý Giá Bán">
                <DataGrid x:Name="dgGoiYDoanhThu" Background="White" BorderThickness="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên Sản Phẩm" Binding="{Binding TenSanPham}" Width="*"/>
                        <DataGridTextColumn Header="Giá Vốn (Gốc)" Binding="{Binding GiaVon, StringFormat=N0}" Width="130"/>
                        <DataGridTextColumn Header="Giá Bán Hiện Tại" Binding="{Binding GiaBanHienTai, StringFormat=N0}" Width="150"/>
                        <DataGridTextColumn Header="Biên Lợi Nhuận (%)" Binding="{Binding TiLeLoiNhuanCu, StringFormat=N2}" Width="150"/>
                        <DataGridTextColumn Header="Giá Gợi Ý (Nên Đổi)" Binding="{Binding GiaGoiY, StringFormat=N0}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

        </TabControl>
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#80FFFFFF" Visibility="Collapsed" CornerRadius="8">
            <TextBlock Text="Đang xử lý..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}"/>
        </Border>
    </Grid>
</Page>