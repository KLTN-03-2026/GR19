<Page x:Class="AppCafebookApi.View.quanly.pages.QuanLyCaiDatView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Cài đặt hệ thống" FontFamily="Segoe UI" Background="#FFF8E1" Loaded="Page_Loaded">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0" Duration="0:0:0.5">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#F44336"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#BDBDBD"/>

        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#6F4E37"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Grid.Row="0" Text="CẤU HÌNH HỆ THỐNG" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}" Margin="0,0,0,20"/>
            <Button Content="🡄 Quay lại" HorizontalAlignment="Right" Style="{StaticResource ActionBtn}" Background="Gray" Click="BtnBack_Click" Margin="0,0,0,20"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tìm nhanh:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <TextBox x:Name="txtSearch" Width="300" Padding="8" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderGrayBrush}" TextChanged="TxtSearch_TextChanged" Tag="Nhập tên hoặc mô tả để tìm..."/>
                <Button Content="🔄 Làm mới" Style="{StaticResource ActionBtn}" Background="#607D8B" Margin="20,0,0,0" Click="BtnRefresh_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10">
            <ListView x:Name="lvCaiDat" BorderThickness="0" Background="Transparent" 
                      HorizontalContentAlignment="Stretch" 
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <Border Background="#F5F5F5" Padding="10,8" Margin="0,15,0,5" CornerRadius="4">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" FontSize="15"/>
                                </Border>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListView.GroupStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0,0,0,1" BorderBrush="#EEEEEE" Padding="5,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4*"/>
                                    <ColumnDefinition Width="5*"/>
                                    <ColumnDefinition Width="90"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,15,0">
                                    <TextBlock Text="{Binding TenCaiDat}" FontWeight="Bold" FontSize="13" Foreground="{StaticResource PrimaryBrownBrush}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding MoTa}" FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>

                                <TextBox Grid.Column="1" Text="{Binding GiaTri, UpdateSourceTrigger=PropertyChanged}" 
                                         Padding="8" VerticalAlignment="Center" Margin="0,0,15,0" 
                                         BorderThickness="1" BorderBrush="{StaticResource BorderGrayBrush}"
                                         TextWrapping="Wrap" AcceptsReturn="True" MaxHeight="80" VerticalScrollBarVisibility="Auto"/>

                                <Button Grid.Column="2" Content="Lưu" Style="{StaticResource ActionBtn}" Background="{StaticResource SuccessBrush}" 
                                        Click="BtnSave_Click" Tag="{Binding}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,15,0,0">
            <Border x:Name="NotificationBorder" Visibility="Collapsed" CornerRadius="6" Padding="15,10" Margin="0,0,0,10">
                <TextBlock x:Name="NotificationText" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#80FFFFFF" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Đang xử lý..." FontSize="16" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>