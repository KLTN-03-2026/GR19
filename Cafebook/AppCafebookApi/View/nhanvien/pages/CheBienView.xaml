<Page x:Class="AppCafebookApi.View.nhanvien.pages.CheBienView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1200"
      Title="CheBienView"
      FontFamily="Segoe UI"
      Background="#FFF8E1"
      Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0" Duration="0:0:0.5">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <SolidColorBrush x:Key="ActionGreenBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="CafeBrown" Color="#6F4E37"/>
        <SolidColorBrush x:Key="CafeOrange" Color="#D27D2D"/>
        <SolidColorBrush x:Key="CafeCream" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="TextGray" Color="#757575"/>
        <SolidColorBrush x:Key="ActionGrayBrush" Color="#BDBDBD"/>

        <Style x:Key="KitchenCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect ShadowDepth="4" BlurRadius="15" Color="#6F4E37" Opacity="0.4"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="MÀN HÌNH CHẾ BIẾN" FontSize="24" FontWeight="Bold" Foreground="{StaticResource CafeBrown}"/>
                    <TextBlock x:Name="lblLastUpdated" Text="(Đang tải...)" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="{StaticResource TextGray}" FontStyle="Italic"/>
                </StackPanel>

                <Button x:Name="btnLichSu" Grid.Column="1" ToolTip="Xem các món đã hoàn thành" 
                        Style="{StaticResource ActionButton}" Background="#757575" 
                        Click="BtnLichSu_Click" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🕒" Margin="0,0,8,0" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="Lịch Sử Hôm Nay" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <ScrollViewer x:Name="MainTabs" Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icAllOrders">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource KitchenCard}" Width="360" Margin="10" VerticalAlignment="Top">
                            <StackPanel>

                                <Grid Background="#FFF3E0" Margin="-10,-10,-10,10">
                                    <Border Padding="12,10">
                                        <StackPanel>
                                            <Grid>
                                                <TextBlock Text="{Binding LoaiDonHienThi}" FontWeight="Bold" FontSize="15" Foreground="{StaticResource CafeOrange}"/>
                                                <TextBlock Text="{Binding IdHoaDon, StringFormat='ID: #\{0\}'}" HorizontalAlignment="Right" Foreground="{StaticResource TextGray}" FontSize="14" FontWeight="SemiBold"/>
                                            </Grid>
                                            <TextBlock Text="{Binding SoBan}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource CafeBrown}" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="0,8"
                                                    Background="Transparent" MouseLeftButtonDown="Border_CongThuc_Click" ToolTip="Click để xem công thức">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                                        <TextBlock TextWrapping="Wrap">
                                                            <Run Text="{Binding TenMon}" FontSize="18" FontWeight="SemiBold"/>
                                                            <Run Text="{Binding SoLuong, StringFormat=' (x\{0\})'}" FontSize="18" FontWeight="Bold" Foreground="#42A5F5"/>
                                                        </TextBlock>

                                                        <Border Background="#E0E0E0" CornerRadius="4" HorizontalAlignment="Left" Padding="6,2" Margin="0,5,0,0">
                                                            <TextBlock Text="{Binding NhomIn}" FontSize="11" FontWeight="Bold" Foreground="#616161"/>
                                                        </Border>

                                                        <TextBlock Text="{Binding GhiChu}" FontStyle="Italic" Foreground="Red" Margin="0,5,0,0" TextWrapping="Wrap">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="Text" Value="{x:Null}">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </Trigger>
                                                                        <Trigger Property="Text" Value="">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <Button Content="BẮT ĐẦU" Style="{StaticResource ActionButton}" Background="#42A5F5" MinWidth="110" Margin="0,0,0,8"
                                                                Click="BtnStartItem_Click" Visibility="{Binding IsChoLam, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                                        <Button Content="XONG MÓN" Style="{StaticResource ActionButton}" Background="#66BB6A" MinWidth="110" Margin="0,0,0,8"
                                                                Click="BtnCompleteItem_Click" Visibility="{Binding IsDangLam, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                                        <Button Content="📄 IN LẠI" Style="{StaticResource ActionButton}" Background="{StaticResource ActionGrayBrush}" MinWidth="110"
                                                                Click="BtnInLai_Click"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Separator Margin="0,10,0,10" Background="#EEEEEE"/>

                                <Grid>
                                    <TextBlock Foreground="{StaticResource TextGray}" FontSize="13">
                                        <Run Text="Lúc: "/>
                                        <Run Text="{Binding Items[0].ThoiGianGoiDisplay, Mode=OneWay}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding ThoiGianCho, Mode=OneWay}" HorizontalAlignment="Right" Foreground="Red" FontWeight="Bold" FontSize="14"/>
                                </Grid>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid x:Name="panelLichSu" Grid.Row="1" Margin="10" Visibility="Collapsed" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F5F5F5" Padding="10" CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnDongLichSu" Grid.Column="0" ToolTip="Quay lại Màn hình Chế biến" 
                            Style="{StaticResource ActionButton}" Background="{StaticResource TextGray}" 
                            Click="BtnDongLichSu_Click" Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⬅" Margin="0,0,5,0" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Quay Lại" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <TextBlock Grid.Column="1" Text="Danh sách các món đã hoàn thành trong ngày" 
                               VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource CafeBrown}"/>

                    <Border Grid.Column="2" Background="White" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1" Padding="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="🔍" VerticalAlignment="Center" Margin="5,0,5,0" Foreground="{StaticResource TextGray}"/>
                            <TextBox x:Name="txtTimKiemLichSu" Grid.Column="1" BorderThickness="0" VerticalAlignment="Center" 
                                     Background="Transparent" FontSize="14" TextChanged="TxtTimKiemLichSu_TextChanged"
                                     Tag="Tìm tên món, số bàn..."/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <DataGrid x:Name="dgLichSu" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                      HeadersVisibility="Column" Background="White" BorderThickness="1,0,1,1" BorderBrush="#E0E0E0"
                      RowHeight="40" FontSize="14" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Thời gian gọi" Binding="{Binding ThoiGianGoiDisplay}" Width="120"/>
                    <DataGridTextColumn Header="Số Bàn / Mã HĐ" Binding="{Binding SoBan}" Width="150" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Tên Món" Binding="{Binding TenMon}" Width="*"/>
                    <DataGridTextColumn Header="Số Lượng" Binding="{Binding SoLuong}" Width="100" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Khu Vực" Binding="{Binding NhomIn}" Width="120"/>
                    <DataGridTextColumn Header="Trạng Thái" Binding="{Binding TrangThai}" Width="120" Foreground="{StaticResource ActionGreenBrush}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="2" Background="#80FFFFFF" Visibility="Collapsed">
            <TextBlock Text="Đang tải..." HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="18" FontWeight="SemiBold" Foreground="White"/>
        </Border>

        <Grid x:Name="CongThucOverlay" Grid.RowSpan="2" Background="#80000000" Visibility="Collapsed">
            <Border VerticalAlignment="Center" HorizontalAlignment="Center"
                    Background="White" CornerRadius="10" Width="420" MaxHeight="500"
                    Padding="10">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="3" BlurRadius="15" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" BorderBrush="#EEE" BorderThickness="0,0,0,1">
                        <TextBlock x:Name="lblCongThucTenMon" Text="Công thức: Món ABC"
                                   FontSize="18" FontWeight="Bold" Foreground="{StaticResource CafeBrown}"
                                   Padding="10,8"/>
                    </Border>

                    <ListView x:Name="lvCongThuc" Grid.Row="1" Margin="5" BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nguyên Liệu" DisplayMemberBinding="{Binding TenNguyenLieu}" Width="180"/>
                                <GridViewColumn Header="Số Lượng" DisplayMemberBinding="{Binding SoLuongSuDung}" Width="80"/>
                                <GridViewColumn Header="Đơn Vị" DisplayMemberBinding="{Binding TenDonVi}" Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <Button x:Name="BtnCloseCongThuc" Grid.Row="2" Content="Đóng"
                            Style="{StaticResource ActionButton}" Background="#757575"
                            Margin="10" Width="80" HorizontalAlignment="Right"
                            Click="BtnCloseCongThuc_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>