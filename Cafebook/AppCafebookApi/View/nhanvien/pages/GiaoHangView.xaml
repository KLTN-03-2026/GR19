<Page x:Class="AppCafebookApi.View.nhanvien.pages.GiaoHangView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1200"
      Title="Quản lý Giao Hàng"
      FontFamily="Segoe UI" Background="#F5F5F5" Loaded="Page_Loaded">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,30,0,0" To="0" Duration="0:0:0.5">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrown" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrown" Color="#4E342E"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#EE4D2D"/>
        <SolidColorBrush x:Key="TextMain" Color="#333333"/>
        <SolidColorBrush x:Key="TextMuted" Color="#757575"/>
        <SolidColorBrush x:Key="BorderGray" Color="#EAEAEA"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#26AA99"/>
        <SolidColorBrush x:Key="DangerRed" Color="#EE4D2D"/>
        <SolidColorBrush x:Key="InfoBlue" Color="#1E88E5"/>
        <SolidColorBrush x:Key="WarningYellow" Color="#FFB300"/>
        <SolidColorBrush x:Key="PurpleBrush" Color="#8E24AA"/>

        <CollectionViewSource x:Key="ShippersSource" />

        <Style x:Key="ShopeeTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="StatusTabs"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0,0,0,3" x:Name="border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentOrange}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentOrange}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentOrange}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaterialButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrown}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource MaterialButton}">
            <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource MaterialButton}">
            <Setter Property="Background" Value="{StaticResource DangerRed}"/>
        </Style>

        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource MaterialButton}">
            <Setter Property="Background" Value="{StaticResource WarningYellow}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkBrown}"/>
        </Style>

        <Style x:Key="BtnChuyenBepStyle" TargetType="Button" BasedOn="{StaticResource WarningButton}">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ xác nhận">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BtnHoanThanhStyle" TargetType="Button" BasedOn="{StaticResource SuccessButton}">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đang giao">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BtnTraHangStyle" TargetType="Button" BasedOn="{StaticResource MaterialButton}">
            <Setter Property="Background" Value="{StaticResource PurpleBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đang giao">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BtnHuyStyle" TargetType="Button" BasedOn="{StaticResource DangerButton}">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ thanh toán">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ xác nhận">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đang chuẩn bị">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="25,20" BorderBrush="{StaticResource BorderGray}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ĐƠN ĐẶT HÀNG ONLINE" FontSize="22" FontWeight="Black" Foreground="{StaticResource PrimaryBrown}" VerticalAlignment="Center"/>
                    <TextBlock Text=" | Ship Nội Bộ" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnConfirmAll" Content="✓ Xác nhận tất cả đơn chờ" Style="{StaticResource SuccessButton}" Click="BtnConfirmAll_Click" Margin="0,0,10,0"/>
                    <Button x:Name="btnRefresh" Content="🔄 Làm mới" Style="{StaticResource MaterialButton}" Background="#F5F5F5" Foreground="{StaticResource TextMain}" BorderBrush="#EAEAEA" BorderThickness="1" Click="BtnRefresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="0,0,0,1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal" x:Name="pnlTabs">
                    <RadioButton x:Name="rbTatCa" Content="Tất cả" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbChoThanhToan" Content="Chờ thanh toán" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbChoXacNhan" Content="Chờ xác nhận" Style="{StaticResource ShopeeTabStyle}" IsChecked="True" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDangChuanBi" Content="Đang chuẩn bị" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbChoLayHang" Content="Chờ lấy hàng" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDangGiao" Content="Đang giao" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDaGiao" Content="Đã giao" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbTraHang" Content="Trả hàng" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDaHuy" Content="Đã hủy" Style="{StaticResource ShopeeTabStyle}" Checked="TabStatus_Checked"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="2" Background="#F5F5F5" Padding="20,15">
            <Border Background="White" CornerRadius="4" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" Height="45">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="15,0,10,0" Foreground="#BDBDBD"/>

                    <TextBox x:Name="txtSearch" Grid.Column="1" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" FontSize="14" TextChanged="TxtSearch_TextChanged" VerticalContentAlignment="Center"/>

                    <TextBlock Grid.Column="1" IsHitTestVisible="False" Text="Bạn có thể tìm kiếm theo Tên Khách Hàng, Số điện thoại hoặc Mã HĐ..." VerticalAlignment="Center" Margin="5,0,0,0" Foreground="#BDBDBD" FontSize="14">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
        </Border>

        <Border Grid.Row="3" Background="White" Margin="20,0,20,20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <DataGrid x:Name="dgGiaoHang" AutoGenerateColumns="False" CanUserAddRows="False"
                      BorderThickness="0" Background="Transparent" RowHeight="85" HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F0F0">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FAFAFA"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#EAEAEA"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Đơn" Binding="{Binding IdHoaDon}" IsReadOnly="True" FontWeight="Black" Foreground="{StaticResource PrimaryBrown}" Width="90"/>
                    <DataGridTextColumn Header="Thời gian" Binding="{Binding ThoiGianTao, StringFormat=dd/MM/yyyy HH:mm}" IsReadOnly="True" Foreground="{StaticResource TextMuted}" Width="130"/>

                    <DataGridTemplateColumn Header="Thông tin khách hàng" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Margin="10,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TenKhachHang}" FontWeight="Bold" FontSize="15" Foreground="{StaticResource TextMain}"/>
                                    <TextBlock Text="{Binding SoDienThoaiGiaoHang, StringFormat=📞 {0}}" Foreground="{StaticResource InfoBlue}" FontWeight="SemiBold" Margin="0,4,0,2"/>
                                    <TextBlock Text="{Binding DiaChiGiaoHang, StringFormat=📍 {0}}" Foreground="{StaticResource TextMuted}" FontSize="13" TextWrapping="Wrap" MaxHeight="38" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0} đ}" IsReadOnly="True" FontWeight="Black" Foreground="{StaticResource DangerRed}" FontSize="15" Width="120"/>

                    <DataGridTemplateColumn Header="Trạng thái" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="12,6" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#F5F5F5"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đã giao">
                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đang giao">
                                                    <Setter Property="Background" Value="#E3F2FD"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ lấy hàng">
                                                    <Setter Property="Background" Value="#E0F7FA"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đang chuẩn bị">
                                                    <Setter Property="Background" Value="#FFF3E0"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ xác nhận">
                                                    <Setter Property="Background" Value="#FFF9C4"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Trả hàng">
                                                    <Setter Property="Background" Value="#F3E5F5"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đã hủy">
                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ thanh toán">
                                                    <Setter Property="Background" Value="#ECEFF1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding TrangThaiGiaoHang, FallbackValue='Chờ xác nhận'}" FontWeight="SemiBold" FontSize="13">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#757575"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đã giao">
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đang giao">
                                                        <Setter Property="Foreground" Value="#1565C0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ lấy hàng">
                                                        <Setter Property="Foreground" Value="#00838F"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đang chuẩn bị">
                                                        <Setter Property="Foreground" Value="#EF6C00"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ xác nhận">
                                                        <Setter Property="Foreground" Value="#F57F17"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Trả hàng">
                                                        <Setter Property="Foreground" Value="#7B1FA2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đã hủy">
                                                        <Setter Property="Foreground" Value="#C62828"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ thanh toán">
                                                        <Setter Property="Foreground" Value="#5D4037"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Shipper Nội Bộ" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding Source={StaticResource ShippersSource}}"
                                          SelectedValue="{Binding IdNguoiGiaoHang, UpdateSourceTrigger=PropertyChanged}"
                                          DisplayMemberPath="TenNguoiGiaoHang"
                                          SelectedValuePath="IdNguoiGiaoHang"
                                          SelectionChanged="CmbNguoiGiaoHang_SelectionChanged"
                                          VerticalAlignment="Center" Padding="8,6" Margin="5"
                                          Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1">
                                    <ComboBox.Style>
                                        <Style TargetType="ComboBox">
                                            <Setter Property="IsEnabled" Value="True"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ thanh toán">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đã giao">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đã hủy">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Trả hàng">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ComboBox.Style>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Tác vụ" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Button x:Name="btnChuyenCheBien" Content="Nhận đơn" Style="{StaticResource BtnChuyenBepStyle}" Click="BtnChuyenCheBien_Click" Tag="{Binding IdHoaDon}" Margin="3"/>
                                    <Button x:Name="btnHoanThanh" Content="Đã giao" Style="{StaticResource BtnHoanThanhStyle}" Click="BtnHoanThanh_Click" Tag="{Binding IdHoaDon}" Margin="3"/>
                                    <Button x:Name="btnTraHang" Content="Trả hàng" Style="{StaticResource BtnTraHangStyle}" Click="BtnTraHang_Click" Tag="{Binding IdHoaDon}" Margin="3"/>
                                    <Button Content="In Phiếu" Style="{StaticResource MaterialButton}" Background="Transparent" Foreground="{StaticResource InfoBlue}" BorderBrush="{StaticResource InfoBlue}" BorderThickness="1" Padding="10,6" Click="BtnInPhieu_Click" Tag="{Binding IdHoaDon}" Margin="3"/>
                                    <Button x:Name="btnHuyDon" Content="Hủy" Style="{StaticResource BtnHuyStyle}" Background="Transparent" Foreground="{StaticResource DangerRed}" BorderBrush="{StaticResource DangerRed}" BorderThickness="1" Padding="10,6" Click="BtnHuyDon_Click" Tag="{Binding IdHoaDon}" Margin="3"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#99FFFFFF" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="{StaticResource AccentOrange}" BorderThickness="0" Background="#E0E0E0"/>
                <TextBlock Text="Đang tải dữ liệu..." FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMuted}" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>