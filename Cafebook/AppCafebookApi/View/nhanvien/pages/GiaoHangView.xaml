<Page x:Class="AppCafebookApi.View.nhanvien.pages.GiaoHangView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Giao Hàng"
      FontFamily="Segoe UI" Background="#FAF6F0" Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,30,0,0" To="0" Duration="0:0:0.5">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>
    
    <Page.Resources>
        <SolidColorBrush x:Key="CoffeeDark" Color="#3E2723"/>
        <SolidColorBrush x:Key="CoffeePrimary" Color="#5D4037"/>
        <SolidColorBrush x:Key="CoffeeLight" Color="#D7CCC8"/>
        <SolidColorBrush x:Key="CreamBackground" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#E65100"/>
        <SolidColorBrush x:Key="TextMain" Color="#212121"/>
        <SolidColorBrush x:Key="TextMuted" Color="#757575"/>
        <SolidColorBrush x:Key="DangerRed" Color="#D32F2F"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#2E7D32"/>
        <SolidColorBrush x:Key="InfoBlue" Color="#1976D2"/>

        <CollectionViewSource x:Key="ShippersSource" />

        <Style x:Key="MaterialTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="StatusTabs"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0,0,0,3" x:Name="border" CornerRadius="4,4,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EFEBE9"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentOrange}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentOrange}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaterialButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CoffeePrimary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="25,15" BorderBrush="{StaticResource CoffeeLight}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ĐƠN GIAO HÀNG" FontSize="24" FontWeight="Black" Foreground="{StaticResource CoffeeDark}" VerticalAlignment="Center"/>
                    <TextBlock Text=" | Giao hàng nội bộ" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Lọc ngày:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold" Foreground="{StaticResource TextMain}"/>
                    <DatePicker x:Name="dpNgayLoc" Width="140" Margin="0,0,20,0" SelectedDateChanged="DpNgayLoc_SelectedDateChanged"/>

                    <Button x:Name="btnConfirmAll" Content="✓ Duyệt tất cả" Style="{StaticResource MaterialButton}" Background="{StaticResource SuccessGreen}" Click="BtnConfirmAll_Click" Margin="0,0,10,0"/>
                    <Button x:Name="btnRefresh" Content="🔄 Làm mới" Style="{StaticResource MaterialButton}" Background="#ECEFF1" Foreground="{StaticResource CoffeeDark}" Click="BtnRefresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource CoffeeLight}" BorderThickness="0,0,0,1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal" x:Name="pnlTabs">
                    <RadioButton x:Name="rbTatCa" Content="Tất cả" Style="{StaticResource MaterialTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbChoThanhToan" Content="Chờ thanh toán" Style="{StaticResource MaterialTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbChoXacNhan" Content="Chờ xác nhận" Style="{StaticResource MaterialTabStyle}" IsChecked="True" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDangChuanBi" Content="Đang chuẩn bị" Style="{StaticResource MaterialTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbChoLayHang" Content="Chờ lấy hàng" Style="{StaticResource MaterialTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDangGiao" Content="Đang giao" Style="{StaticResource MaterialTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDaGiao" Content="Đã giao" Style="{StaticResource MaterialTabStyle}" Checked="TabStatus_Checked"/>
                    <RadioButton x:Name="rbDaHuy" Content="Đã hủy" Style="{StaticResource MaterialTabStyle}" Checked="TabStatus_Checked"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="2" Background="Transparent" Padding="20,15">
            <Border Background="White" CornerRadius="8" Height="45">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="5" ShadowDepth="1"/>
                </Border.Effect>
                <Grid>
                    <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="15,0,0,0" HorizontalAlignment="Left"/>
                    <TextBox x:Name="txtSearch" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" FontSize="15" TextChanged="TxtSearch_TextChanged" Margin="45,0,10,0"/>
                    <TextBlock IsHitTestVisible="False" Text="Tìm kiếm theo Mã HĐ, Tên hoặc Số điện thoại..." VerticalAlignment="Center" Margin="48,0,0,0" Foreground="{StaticResource TextMuted}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
        </Border>

        <Border Grid.Row="3" Background="White" Margin="20,0,20,20" CornerRadius="12" Padding="5">
            <Border.Effect>
                <DropShadowEffect Color="#3E2723" Opacity="0.1" BlurRadius="10" ShadowDepth="3"/>
            </Border.Effect>
            <DataGrid x:Name="dgGiaoHang" AutoGenerateColumns="False" CanUserAddRows="False"
                      BorderThickness="0" Background="Transparent" RowHeight="80" HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F5F5F5">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource CreamBackground}"/>
                        <Setter Property="Foreground" Value="{StaticResource CoffeeDark}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="15,12"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Đơn" Binding="{Binding IdHoaDon, StringFormat=HĐ-{0:D4}}" IsReadOnly="True" FontWeight="Black" Foreground="{StaticResource CoffeePrimary}" Width="100"/>

                    <DataGridTemplateColumn Header="Khách hàng" Width="2.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Margin="10,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TenKhachHang}" FontWeight="Bold" FontSize="15" Foreground="{StaticResource TextMain}"/>
                                    <TextBlock Text="{Binding SoDienThoaiGiaoHang, StringFormat=📞 {0}}" Foreground="{StaticResource InfoBlue}" FontWeight="SemiBold" Margin="0,4,0,2"/>
                                    <TextBlock Text="{Binding DiaChiGiaoHang, StringFormat=📍 {0}}" Foreground="{StaticResource TextMuted}" FontSize="13" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0} đ}" IsReadOnly="True" FontWeight="Black" Foreground="{StaticResource AccentOrange}" FontSize="15" Width="120"/>

                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThaiGiaoHang}" IsReadOnly="True" FontWeight="Bold" Foreground="{StaticResource CoffeePrimary}" Width="130"/>

                    <DataGridTemplateColumn Header="Điều phối Shipper" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding Source={StaticResource ShippersSource}}"
                                          SelectedValue="{Binding IdNguoiGiaoHang, UpdateSourceTrigger=PropertyChanged}"
                                          DisplayMemberPath="TenNguoiGiaoHang" SelectedValuePath="IdNguoiGiaoHang"
                                          SelectionChanged="CmbNguoiGiaoHang_SelectionChanged"
                                          VerticalAlignment="Center" Padding="8,6" Margin="5" Background="White">
                                    <ComboBox.Style>
                                        <Style TargetType="ComboBox">
                                            <Setter Property="IsEnabled" Value="False"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ lấy hàng">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ComboBox.Style>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Thao tác" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                                    <Button x:Name="btnNhanDon" Content="✅ Nhận đơn" Click="BtnChuyenCheBien_Click" Tag="{Binding IdHoaDon}" Margin="3">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource MaterialButton}">
                                                <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ xác nhận">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <Button x:Name="btnHuy" Content="❌ Hủy" Click="BtnHuyDon_Click" Tag="{Binding IdHoaDon}" Margin="3">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource MaterialButton}">
                                                <Setter Property="Background" Value="{StaticResource DangerRed}"/>
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ thanh toán">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Chờ xác nhận">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <Button x:Name="btnIn" Content="🖨️ In Phiếu" Click="BtnInPhieu_Click" Tag="{Binding IdHoaDon}" Margin="3" Style="{StaticResource MaterialButton}" Background="#455A64"/>

                                    <Button x:Name="btnXemAnh" Content="🖼️ Xem ảnh" Click="BtnXemAnh_Click" Tag="{Binding AnhGiaoHang}" Margin="3">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource MaterialButton}">
                                                <Setter Property="Background" Value="{StaticResource InfoBlue}"/>
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Đã giao">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TrangThaiGiaoHang}" Value="Hoàn thành">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#AAFFFFFF" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="{StaticResource AccentOrange}"/>
                <TextBlock Text="Đang xử lý..." FontWeight="Bold" Foreground="{StaticResource CoffeeDark}" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>