<Page x:Class="AppCafebookApi.View.nhanvien.pages.LichLamViecView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Lịch làm việc của tôi" FontFamily="Segoe UI" Background="#FFFDF8" Loaded="Page_Loaded">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0" Duration="0:0:0.5">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#D27D2D"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#E0E0E0"/>

        <DropShadowEffect x:Key="SoftShadow" ShadowDepth="2" BlurRadius="10" Opacity="0.1" Color="Black"/>

        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="📅 Lịch Làm Việc Của Tôi" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,20" Effect="{StaticResource SoftShadow}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="🡄 Trước" Style="{StaticResource ActionBtn}" Background="#757575" Click="BtnPrev_Click" Margin="0,0,15,0"/>

                <Border Background="#FFF3E0" CornerRadius="8" Padding="20,8" Margin="0,0,15,0">
                    <TextBlock x:Name="txtKhungThoiGian" Text="Đang tải..." FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}" VerticalAlignment="Center"/>
                </Border>

                <Button Content="Sau 🡆" Style="{StaticResource ActionBtn}" Background="#757575" Click="BtnNext_Click" Margin="0,0,30,0"/>

                <Button Content="Chỉ xem Hôm Nay" Style="{StaticResource ActionBtn}" Background="#4CAF50" Click="BtnToday_Click" Margin="0,0,10,0"/>
                <Button Content="Xem cả Tuần Này" Style="{StaticResource ActionBtn}" Background="{StaticResource AccentOrangeBrush}" Click="BtnThisWeek_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Effect="{StaticResource SoftShadow}" ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="65"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="0,0,1,1" Background="#F5F5F5"/>

                <Grid x:Name="gridHeaderDays" Grid.Row="0" Grid.Column="1" Background="#F5F5F5">
                </Grid>

                <ScrollViewer Grid.Row="1" Grid.ColumnSpan="2" VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="65"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="0,0,1,0">
                            <Canvas x:Name="canvasTime" Background="Transparent"/>
                        </Border>

                        <Canvas x:Name="canvasSchedule" Grid.Column="1" Background="Transparent" SizeChanged="CanvasSchedule_SizeChanged"/>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border x:Name="LoadingOverlay" Grid.RowSpan="3" Background="#80FFFFFF" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Đang tải lịch làm việc..." HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBrownBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>